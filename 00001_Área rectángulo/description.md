Ahora queremos poder calcular el área del rectángulo, que es su `base * altura`. 

> Declará la función `area`, que tome un rectángulo y denote su área. Por ejemplo, la expresión: 
> 
> ```puppet
> area(Rectangulo(base <- 2, altura <- 3))
> ```
> 
> debe denotar `6`